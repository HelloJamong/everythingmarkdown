# EverythingMarkdown To-do

- 최종 정리일: 2026-09-08
- 현재 상태: P0 구현 진행 중이며, 로컬 Linux 테스트와 시험 빌드는 통과했다. Windows/macOS 최종 검증은 남아 있다.
- 관련 문서: [진행 현황](PROGRESS.md) · [구현 설계서](docs/design.md) · [검증 명세](docs/test-spec.md)
- 순서: **P0 최소 배포 검증 → P1 공통 코어 → P2 CLI → P3 GUI → P4 통합·배포 검증**.
- P0 실행 근거: [검증 기록](docs/p0-validation.md). 시험 빌드와 정식 외부 배포는 구분한다.

## 완료한 준비 작업

- [x] 이전 대화의 프로젝트 요구사항 R1~R7 정리.
- [x] 기술 대안 비교와 공식 MarkItDown API 근거 조사.
- [x] GUI/CLI 공통 구조, 저장·오류·배포 정책 설계.
- [x] 수용 기준 A01~A26 및 계층별 검증 방식 작성.
- [x] Architect 보완 검토와 후속 Critic 검토 완료.
- [x] 진행 현황 및 단계별 To-do 문서 작성.

## P0. 최소 배포 검증 — 가장 먼저

목적: 본 구현에 앞서 선택한 기술로 목표 OS에서 실제 실행·변환·배포가 가능한지 확인한다.

- [x] 초기 포맷 7개, 100 MiB 상한, Windows/macOS OS·CPU 제안 범위를 재확인하고 지원표에 기록한다.
- [x] Windows/macOS 빌드·실행 환경의 가용성을 확인한다. 사용할 수 없는 플랫폼은 미검증으로 기록한다.
- [x] 프로젝트 가상환경과 최소 패키지 설정을 준비한다. 명시된 의존성 범위를 준수하고 불필요한 라이브러리를 추가하지 않는다.
- [ ] MarkItDown 0.1.7, 포맷 extras, Python/Tcl/Tk 및 패키저 호환성을 확인한다.
- [x] 개인정보 없는 최소 PDF/DOCX/PPTX/XLSX/HTML/CSV/TXT fixture를 준비한다.
- [ ] 최소 Tk 창·터미널 입출력·단일 컨버터 어댑터로 OS별 시험 배포물을 만든다. Linux 시험 빌드는 생성했으나 Windows/macOS는 미실행이다.
- [ ] 각 목표 OS의 Python 미설치 환경에서 GUI 창·CLI 입출력·7개 포맷 변환을 확인한다.
- [ ] Finder/Explorer/터미널 실행 시 실제 CWD와 쓰기 권한을 기록한다. 저장 위치를 묵시적으로 변경하지 않는다.
- [ ] 결과와 실패 원인을 기록하고, 통과한 패키지 버전·전이 의존성 잠금 방식을 확정한다.

**완료 기준:** 목표 환경별 시험 배포 증거가 있고, 포맷/플랫폼 실패가 해소되거나 명시적인 설계 변경으로 정리됨. 미검증 플랫폼을 성공으로 취급하지 않는다.

### 이번 P0 진행 증거 (2026-09-08)

- Python 3.12.13 가상환경, MarkItDown 0.1.7, PyInstaller 6.22.2 설치 및 `uv.lock` 생성.
- 7개 자체 제작 fixture, 명시 컨버터 프로브, 최소 Tk 창, OS별 실행 가능한 빌드 스크립트 작성.
- Linux에서 소스/동결 CLI 7개 포맷 변환, 테스트 15개 통과. 네트워크 격리 환경에서도 재검증.
- Linux GUI 패키징의 Tcl/Tk 라이브러리 누락을 보완. 창 표시는 디스플레이 부재로 미검증.
- `uv.lock`은 현재 환경의 재현용 초기 잠금이다. 목표 OS 검증 전 최종 호환 버전 확정이 아니다.
- Windows/macOS 빌드·Python 미설치 실행·Finder/Explorer CWD 검증은 환경 부재로 미실행.
- 위 지원 범위 체크는 **제안 범위를 시험 대상으로 기록한 것**이며 정식 지원 확정이 아니다.

## P1. 공통 변환 코어

선행 조건: P0 완료. 예정 파일: `src/everythingmarkdown/{models,service,markitdown_adapter}.py`, `tests/unit/`.

- [ ] 요청·결과 데이터 모델과 안정적인 공통 오류 코드를 작성한다.
- [ ] 시작 CWD 고정, 상대 경로 해석, basename 보존 및 `convert_result` 경로 계산을 구현한다.
- [ ] 일반 파일·확장자·크기·심볼릭 링크·ZIP 위장·OOXML 구조 검증을 구현한다.
- [ ] 자동 builtins/plugins 비활성화, 포맷별 단일 컨버터 등록, `convert_local()`/`result.markdown` 어댑터를 구현한다.
- [ ] 빈 결과 거부, UTF-8·LF 정규화, 배타적 생성, 기존 결과 보존을 구현한다.
- [ ] 쓰기·닫기 실패 시 이번 요청의 미완성 결과만 정리하고 정리 실패 경고를 제공한다.
- [ ] 입력 경계와 파일 안전성 테스트를 우선 작성·실행한다(A06~A16, A22~A26).

**완료 기준:** 가짜 엔진을 사용한 코어 단위 테스트 통과, 실제 엔진과의 최소 통합 확인, 기존 파일 손상 없음. GUI/CLI에 저장 로직을 중복 구현하지 않는다.

## P2. CLI

선행 조건: P1 완료. 예정 파일: `cli.py`, `__main__.py`, 패키지 진입점.

- [ ] 파일 인자, `--help`, `--version` 및 GUI와 구분된 CLI 진입점을 제공한다.
- [ ] 인자 없는 TTY 실행에서 파일 경로 입력을 받고 비TTY에서는 즉시 사용법 오류로 종료한다.
- [ ] 성공 경로는 stdout, 프롬프트·경고·오류는 stderr로 분리한다.
- [ ] 종료 코드 0/2/3/4/130, Ctrl+C·EOF·빈 입력 처리를 구현한다.
- [ ] 인자/대화형 입력, 한글·공백 경로, CWD, 스트림·종료 코드 테스트를 실행한다(A03/A04/A06/A07/A19).

**완료 기준:** 동일 공통 서비스로 인자/대화형 변환이 동작하고 자동화 환경에서 입력 대기에 걸리지 않음.

## P3. GUI

선행 조건: P1 공통 계약 안정화, 기본 진행 순서는 P2 다음. 예정 파일: `gui.py`, `DESIGN.md`.

- [ ] 기존 설계의 GUI 계약을 바탕으로 `DESIGN.md`를 작성한다.
- [ ] 파일 선택, 예상 결과 절대 경로, 상태 안내, 변환 버튼을 구현한다.
- [ ] 파일 미선택·선택 완료·진행·성공·실패 상태와 버튼 활성화를 구현한다.
- [ ] 작업 스레드와 큐/메인 루프를 연결하고 중복 요청·워커의 직접 UI 조작을 막는다.
- [ ] 선택 취소 시 기존 선택 유지, 오류 후 재시도, 작업 중 닫기 예약을 구현한다.
- [ ] 키보드 탐색, 경로 복사, 배율 대응과 UI 응답성을 확인한다(A05/A17/A18).
- [ ] GUI와 CLI에서 동일 입력의 결과 위치·이름·내용이 일치하는지 확인한다(A02).

**완료 기준:** 변환 중에도 UI 이벤트가 처리되고, 성공·실패·재시도·닫기 상태가 명세대로 동작함.

## P4. 통합·패키징·배포 준비

선행 조건: P1~P3 완료. 예정 파일: `tests/integration/`, `packaging/*`, `README.md`, 검증 기록.

- [ ] 실제 엔진으로 7개 포맷의 핵심 텍스트 보존과 한계를 확인한다(A01/A12/A13/A24).
- [ ] 외부 참조 HTML 등 모든 fixture에 네트워크 시도 탐지와 네트워크 차단 검증을 수행한다(A21).
- [ ] 동일 이름 경쟁·권한·디스크/닫기 오류·정리 실패·원본 보존을 통합 검증한다(A09/A10/A14~A16/A22).
- [ ] 최종 GUI/CLI 배포물을 각 OS에서 별도 빌드하고 Python 미설치 환경에서 재검증한다(A20).
- [ ] Tcl/Tk·포맷별 데이터·동적 import 누락, Finder/바로가기 CWD와 OS 배율 문제를 확인한다.
- [ ] 단위·통합 테스트 및 정적 검사를 실행한다. lint/typecheck 도구·설정·명령을 확정하고 결과를 기록한다.
- [ ] 사용법, 지원표, 저장 위치, 오류 해결, OCR/품질 한계, 미서명 빌드 경고를 README에 작성한다.
- [ ] 서드파티 라이선스·고지, 코드서명·공증 필요사항과 준비 상태를 확인한다. 자격증명 사용·외부 게시는 별도 범위로 관리한다.
- [ ] A01~A26별 통과/실패/미실행과 근거를 기록하고, 남은 실패를 해결한 뒤 진행 문서를 갱신한다.

**완료 기준:** 목표 지원표 전체의 테스트·배포 증거가 확보되고, 알려진 제약과 배포 준비 상태가 문서화됨. 로컬 시험 빌드와 정식 서명·외부 릴리스 완료는 구분한다.

## 후속 검토 목록 — MVP 필수 작업 아님

아래 기능은 현재 설계 범위를 자동 확장하지 않으며 별도 필요성 검토 대상이다.

- [ ] GUI 작업 폴더 선택 및 Finder 더블클릭 저장 UX 개선 검토.
- [ ] 일괄 변환, 폴더 입력, 드래그앤드롭 검토.
- [ ] 덮어쓰기 확인 또는 충돌 시 이름 변경 정책 검토.
- [ ] OCR, 이미지/오디오, 레거시 Office 등 지원 포맷 확대 검토.
- [ ] 처리 중 취소, 하드 타임아웃, 프로세스 격리·자원 제한 검토.
- [ ] macOS Intel, Windows ARM64 및 이전 OS 지원 확대 검토.

## 완료 표시와 증거 규칙

- `[x]`는 산출물과 해당 완료 기준의 확인 근거가 있을 때만 표시한다.
- 테스트는 명세 작성, 코드 작성, 실제 실행 성공을 각각 구분한다.
- 검증 기록에는 실행 명령, 날짜, OS/CPU/패키지 버전, 종료 코드, 로그·캡처 위치를 남긴다.
- 플랫폼 환경이 없으면 실패 또는 미실행 사유를 기록하고 해당 단계를 완료로 표시하지 않는다.
- 제품 계약을 변경해야 하면 먼저 설계서·테스트 명세를 갱신하고 변경 검토를 남긴다. 이 목록으로 기존 요구사항을 조용히 바꾸지 않는다.
